name: Issue Events
on:
  label:
    types: [created]

jobs:
  process_mobile_build_request:
    runs-on: ubuntu-latest
    steps:
      - run: echo "label changed for ${{ github.event.repository.issues_url }}"
